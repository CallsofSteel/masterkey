{
  "subcategory": "sound-effects-audio",
  "category": "media",
  "unit": "per call",
  "_note": "Curated by claude-main. Sparse: 82 candidates, only 1 genuine SFX generator. Dropped: TTS, ElevenLabs music/isolation/dubbing, orbisapi audio-analysis (waveform/fingerprint/sentiment), and noise (FDA Adverse Events, Sound Healing, Speed Of Sound physics, business APIs). resolved = agentcash.",
  "entries": [
    {
      "name": "ElevenLabs Sound Effects",
      "provider": "ElevenLabs",
      "providerId": "elevenlabs",
      "description": "ElevenLabs Sound Generation — generate sound effects from a text description, with looping and duration control.",
      "tags": [
        "sound-effects",
        "text-to-audio",
        "foley"
      ],
      "modality": {
        "input": [
          "text"
        ],
        "output": [
          "audio"
        ]
      },
      "backends": [
        0
      ],
      "resolved": {
        "0": {
          "amount": 0.15,
          "method": "POST"
        }
      },
      "usage": {
        "status": "verified",
        "verifiedAt": "2026-06-08",
        "resultPull": "sync",
        "auth": "none",
        "callShape": "POST https://x402.orth.sh/elevenlabs/v1/sound-generation with JSON body {text} (optional: duration_seconds, loop, prompt_influence, model_id; optional query ?output_format=)",
        "inputExample": {
          "text": "a short gentle chime",
          "duration_seconds": 0.5
        },
        "outputShape": "Response body IS the raw audio file (Content-Type: audio/mpeg). No JSON wrapper — write the bytes directly to a .mp3 file.",
        "quirks": [
          "Response is raw binary MP3 audio (audio/mpeg), NOT JSON — save the body bytes directly to a .mp3 file.",
          "duration_seconds minimum is 0.5.",
          "Only 'text' is required; other body fields (loop, duration_seconds, prompt_influence, model_id) are optional."
        ],
        "needs": [],
        "needsApproval": false,
        "guide": "POST to https://x402.orth.sh/elevenlabs/v1/sound-generation over x402 (pays ~$0.15 USDC on Base) with a JSON body containing at minimum {\"text\": \"<description of the sound>\"}. Optionally set duration_seconds (min 0.5), loop, prompt_influence, or model_id, and an output_format query param. The response is the raw audio file itself (Content-Type audio/mpeg) — do not parse it as JSON; write the response bytes straight to a .mp3 file. Plain synchronous call, no auth beyond the x402 payment.",
        "costObservedUsd": 0.15
      },
      "status": "active"
    }
  ]
}
